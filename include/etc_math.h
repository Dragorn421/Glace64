#ifndef ETC_MATH_H
#define ETC_MATH_H

// whatever math routines, not related to anything in particular.

#include <math.h>

int min(int a, int b);
float clamp(float value, float min, float max);

#endif // !ETC_MATH_H
